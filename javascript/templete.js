const person = "Adam boos";
const person2 = "Ben";
const person3 = `Donal Trump`;

const multiline = `frist name,
last name
`

// console.log(multiline);

const a = 20;
const b = 30;

const summary = 'sum of : ' + a + 'and' + b + 'is' + (a+b);

// console.log(summary);

const newSummary = `sum of ${a} and ${b} is ; ${a+b}`; // using backticks

console.log(newSummary);